monitor.alsa.rules = [
  {
    matches = [
      {
        device.name = "~alsa_card.*"
      }
    ]
    actions = {
      update-props = {
        resample.quality = 10
        api.alsa.period-size = 256
        api.alsa.headroom = 1024
      }
    }
  }
]
